# 迭代决策与效果记录

本文档用于记录每次改进、调参、验证或方向切换的原因和实际效果。它服务于后续复盘和探索报告，不替代 `BASELINE_IMPLEMENTATION_PLAN.md`。

## 核心原则

- 先保证可回退：每次修改前确认当前本地基线已推送到 GitHub；没有稳定远端基线时，不继续做可能影响准确率的改动。
- 只让确定性代码做确定的事：记忆管理、检索、候选裁剪、token 预算、输出解析、fallback 和防注入由 Harness 控制；LLM 只在受控候选集合内做一次语义判断。
- 不为了 DEV 分数堆复杂度：只有实验收益明确、机制通用、不会明显增加限流和 token 风险时，才考虑把变体落入 `solution.py`。
- 负向实验也算结论：单轮显著低于当前基线区间的变体不再追加 4 轮确认；只有超过基线或提交前稳定性检查才跑多轮。
- 不把 API key、临时脚本或本地诊断逻辑写进提交文件；命令记录必须脱敏。
- 输出必须可控：最终返回必须来自训练 label allowlist；prompt injection 文本只作为待分类数据，不作为指令。
- 避免私有集风险：优先保留通用 Harness 机制，避免手写 DEV 客服领域规则、过度拟合错误样本或用检索覆盖模型判断。
- 默认单次调用：不默认启用 self-consistency、pairwise tournament 或多 agent；额外调用只在低置信、非法输出或正式风险明确时作为条件触发备选。

## 探索主线总览

| 探索主线/实验 | 跑的 Harness | 结果 | 分析 | 决策 |
| --- | --- | --- | --- | --- |
| 主线增强基线 | `solution.py` 中的增强版检索分类 Harness：BM25/字符 n-gram/label 名称混合检索、任务路由、候选裁剪、few-shot、token 预算、防注入、allowlist parser、本地 fallback，默认单次 LLM 调用。 | 单轮 80.5%；prompt/条 451；completion/条 3.9；无 API 错误、无截断 warning。 | 相比朴素 prompt，主要工程能力已经覆盖；token 和输出合法性不是瓶颈。 | 作为当前稳定基线保留。 |
| 调用链路错误分型 | 同一稳定基线，加临时追踪脚本记录候选命中、raw response、parser、fallback、latency、token 和 API 异常；不改 `solution.py`。 | 80.5%；`candidate_hit_llm_wrong=74`，`candidate_miss=31`，`api_error=0`，`parser_missed_gold=0`。 | 失败主要来自候选命中后的模型误判和少量候选 miss，不是 API、parser 或 token 预算问题。 | 后续不优先修 API/parser/token；转向候选召回小步实验和候选内判别。 |
| 候选扩张与规则调参 | 内存子类变体：扩大候选和示例数量，验证候选召回提升是否能转化为准确率。 | `top16 + 12 examples` 单轮 79.6%；候选命中率从 94.2% 到 95.5%，gold 示例覆盖到 92.4%，prompt 平均 525。 | 检索指标变好，但更大的候选/示例集合干扰 Qwen3-8B 判断，准确率下降。 | 不落入 `solution.py`；不继续全局扩候选。 |
| Prompt 规则加法 | 内存子类变体：在 prompt 中加入更宽泛的判别规则，不改检索。 | 单轮 78.7%。 | 泛化自然语言规则增加模型负担，不能稳定减少候选内误判。 | 不落入 `solution.py`；不继续堆全局 prompt 规则。 |
| 检索高置信覆盖 | 只读诊断网格：当检索 top1 分数足够高时尝试覆盖 LLM 输出。 | 无阈值组合超过 80.5% 基线。 | fallback top1 本身只有约 53.2%，检索高分不等于语义正确；用检索覆盖模型会引入新错。 | 不落入 `solution.py`；传统检索只保留为候选生成和非法输出 fallback。 |
| 稳定基线四轮确认 | 未改代码的主线增强基线，`--runs 4 --workers 4`。 | 平均 80.4%；四轮为 80.3%/80.3%/80.5%/80.3%；无 API 错误、无截断。 | 基线波动很小；80.3%-80.5% 可作为后续单轮探索的比较区间。 | 之后日常探索只跑 1 轮；只有超过基线或提交前确认才跑 4 轮。 |
| 局部提示与轻量混淆对比 | 内存子类变体：对候选 label 附加近邻训练样本压缩提示、共享 label token contrast，或调整 examples 顺序。 | 三个单轮分别为 78.5%、79.2%、78.5%。 | 额外局部信息或顺序变化都会增加 prompt 噪声，小模型没有稳定获益。 | 不落入 `solution.py`；停止这类加料 prompt。 |
| 提交风险整理 | 当时的文档决策：在已有负向实验基础上，先转向提交前风险检查、报告整理和提交准备。 | 未运行新评测；沿用已有实验结果。 | 当时 prompt 远低于 2048；候选扩张、规则加法和局部对比都证明继续增加或重排 prompt 信息会降分。prototype 的主要价值被判断为节省 token 和压缩 label memory。 | 当时未改 `solution.py`；后续 Label Prototype / Concept Memory 探索重新打开并找到正向组合。 |
| Listwise 候选重排 | 内存子类变体：把分类 prompt 改成“对候选 label 做 listwise ranking 后只返回 rank-1”，分别测试同候选、更多候选少示例、候选-only。 | 三个单轮分别为 77.7%、77.4%、68.6%；无 API 错误、无截断。 | 同候选也明显降分，说明问题不是召回，而是 reranking 话术和枚举 ranking 任务让 Qwen3-8B 判断更不稳；候选-only 说明 label 名称本身不足以表达类别语义。 | 不落入 `solution.py`；不追加 4 轮；作为已探索但不采用方向写入报告。 |
| Label Prototype / Concept Memory | 内存子类变体：从训练样本代码生成 label memory cards，测试替代 examples、行内 cues、更多候选+少示例三种形态。 | 替代 examples 78.1%；行内 cues+examples 78.9%；更多候选+memory cards+4 examples 为 80.891%，4 轮确认均为 80.891%。 | prototype 不能替代 examples，轻量 cues 也不够；有效组合是用 label cards 支撑更多候选，同时保留少量原始 evidence。prompt 升到 1074/条但仍低于 2048，无截断。 | 已落入 `solution.py`，成为当前主线增强；后续只做风险检查或进一步泛化验证。 |
| 共享数据侧增强审核 | 只读审核 `data/data.zip`；把新增 MCQ/OOD 当独立泛化验证集运行当前 Harness；再分别测试“直接合并共享 train 到 DEV”和“只合并同名 label 样本”。 | MCQ 独立 73.2%；OOD 独立 78.0%；直接合并共享 train 后 DEV 80.1%；仅同名 `exchange_rate` 3 条增强后 DEV 81.1%。 | 新增数据格式干净且能作为外部分布验证集；但直接合并会把 55 个纯外部 label 混进候选，468/539 个 DEV 样本出现外部候选，gold 候选命中从 95.55% 降到 94.25%。同名 label 小幅提升只多约 1 个正确样本，证据薄且有语义污染和硬编码风险。 | 不把共享数据直接落入 `solution.py`；可作为泛化验证和报告探索材料。后续若用数据侧思路，只允许训练流内自适应，不读取或硬编码外部文件。 |
| 隔离式 MCQ 路由 | 只在训练 label 是 A-H 选项且文本像选择题时触发专用无示例选择题 prompt；非法输出、超预算或调用异常则回退原通用 Harness。 | 临时三变体：无示例 74.74%、解析选项 71.35%、每选项一例 70.57%；落入 `solution.py` 后 MCQ 75.5%，DEV 80.9%。 | 选择题不需要从训练例中学习 label 语义，examples 和过强解析话术反而干扰；无示例直接读题能提升 MCQ，并把 prompt/条从 628 降到 189。DEV 不触发该分支。 | 已落入 `solution.py`；作为隔离增强保留，后续不继续为 MCQ 加复杂规则，除非同时回测 DEV 不掉。 |

## 主线推进逻辑

先建立通用稳定基线，因为提交文件需要的不只是 DEV 准确率，还包括 token 预算、输出合法性、防注入、并发安全和 fallback。基线单轮 80.5%，且没有 API 或截断问题，所以后续不应先修基础设施，而应先诊断错误来源。

调用链路错误分型显示主要错误不是调用失败，也不是 parser 或 token 超限，而是候选命中后模型仍选错，以及少量候选 miss。因此先验证“候选更多、示例更多、规则更多、检索更强势”是否能解决问题。

候选扩张与规则调参的结果说明，离线召回提升不能直接转化为线上准确率；对 Qwen3-8B 来说，更大的候选集、更宽泛的规则和检索 override 都会带来新误判。于是探索范围收敛到更局部的 contrast / confusion-aware 小实验，并且默认单轮验证。

局部提示与轻量混淆对比仍然降分，说明当前瓶颈不能靠简单增加 label hints、共享 token 对比或示例重排解决。继续沿这个方向细刷 DEV，收益不确定，过拟合和 prompt 噪声风险更高。

Listwise 候选重排说明，把同一候选集包装成 ranking 任务不会自动提升候选内判别。Label Prototype / Concept Memory 的后续探索则给出了一个更具体的正向组合：不是用 prototype 替代 examples，也不是给候选行加少量 cues，而是用代码生成的 label memory cards 支撑更多候选，同时保留少量原始 examples。这个组合把 prompt 从 451/条提高到约 1074/条，但仍在 2048 预算内，并在 4 轮确认中稳定达到 80.891%。因此当前主线从“纯 few-shot 检索分类”升级为“候选检索 + label memory cards + 少量 evidence examples”的单调用 Harness。

共享数据侧增强审核把探索从 DEV 内部调参转到外部分布风险检查。`data.zip` 中新增 MCQ 和 OOD 数据没有文本重复或格式问题，适合作为“选择题长文本”和“非银行多域意图”的泛化验证集；但它们的 label 空间与 DEV 不同，不能作为当前任务的普通训练增强。直接把共享 train 合并到 DEV 会扩大 allowlist 并污染候选集，实际单轮也从 80.9% 降到 80.1%。只合并同名 `exchange_rate` 的 3 条样本虽然单轮到 81.1%，但收益仅约 1 个样本，且来自共享外部样本，不符合提交时不读外部文件、不过度依赖特定共享数据的原则。因此共享数据的结论是“用于验证和写报告，不进入提交 Harness”。

隔离式 MCQ 路由是在共享数据审核后的针对性增强：不把外部 MCQ 数据并入训练记忆，而是把 MCQ 作为一种可识别任务形态处理。实验显示，选择题 label `A/B/C/D` 本身没有语义，当前通用 few-shot 分类会把训练 examples 当作类别证据引入噪声；专用无示例 prompt 直接让 LLM 阅读题干和选项，成本更低且更准。该分支只在 choice label set 和 choice-like text 同时满足时触发，失效后回退通用 Harness，因此不会影响普通 DEV 路径。

## 维护规则

- 每次修改 `solution.py` 的策略、prompt、检索、token 预算、输出解析、fallback 或并发处理后，都要新增一条记录。
- 每次验证运行后，都要把命令、准确率、token、耗时、warning 和错误情况补到对应记录。
- 未运行验证时，实际效果必须写为“待验证”，不要用主观判断冒充实验结论。
- 如果方向被放弃，要写清楚放弃原因、观察到的问题和替代方案。
- 记录应保留旧结论，后续修正用新条目说明，不静默覆盖历史判断。

## 记录模板

```markdown
### YYYY-MM-DD - 标题

- 背景/触发：
- 决策/改动：
- 原因：
- 预期效果：
- 实际效果：
- 验证命令：
- 指标：
- 影响文件：
- 下一步：
```

## 当前基线状态

截至 2026-05-08 隔离式 MCQ 路由探索后，服务器目录中的 `solution.py` 已不是朴素示例实现，而是带 label memory cards 和隔离选择题分支的主线增强版检索分类 Harness：

- `update()` 调用 `super().update(text, label)` 保存训练样本，并维护 label 首次出现顺序、label allowlist 和索引 dirty 状态。
- `predict()` 会懒构建只读索引；若训练 label 是 A-H 选择题选项且文本像选择题，则先走隔离式无示例 MCQ prompt，失效后回退通用路径；其余样本执行任务路由、候选检索、label memory card 构造、few-shot 示例选择、token 预算内 prompt 构造、LLM 单次分类、输出解析和本地 fallback。
- 已实现训练 label allowlist、BM25/字符 n-gram/label 名称混合检索、候选裁剪、代码生成 label prototype / concept memory、隔离式选择题路由、少 label/疑似 prompt injection 路由、防注入边界、输出解析和 fallback。
- `llm_client.py` 已改为通过环境变量配置本地百炼 OpenAI-compatible API，API key 不落盘；百炼 Qwen3 非思考模式参数使用 `extra_body={"enable_thinking": False}`。
- 已用百炼北京端点、`qwen3-8b`、`--runs 1 --workers 4`、零重试完成隔离式 MCQ 路由落代码后验证：DEV 准确率 80.9%，prompt/条 1074 token，completion/条 3.9 token，总耗时 65.8s；MCQ 准确率 75.5%，prompt/条 189 token，completion/条 1.5 token，总耗时 42.0s；均无 prompt truncated warning、无 API 错误。

## 记录

### 2026-05-07 - 建立文档治理和迭代记录

- 背景/触发：用户要求了解目录，重点关注文档，修正 `AGENT.md`；后续生成新文档时必须在 `AGENT.md` 中说明；还需要维护一份文档记录每次改进或切换方向的原因和实际效果。
- 决策/改动：更新 `AGENT.md`，补充服务器目录状态说明、顶层目录速览、文档台账、文档管理规则、必读顺序和验证记录要求；新增 `迭代决策记录.md` 作为长期追加的决策与效果记录。
- 原因：原 `AGENT.md` 已有实现约束，但缺少文档台账和新增文档登记规则；现有 `初步信息汇总.md` 与 `BASELINE_IMPLEMENTATION_PLAN.md` 偏方案分析和实施计划，不适合承接每次实验的流水记录。
- 预期效果：后续 Agent 能先通过 `AGENT.md` 理解目录和文档职责；新增文档不再失控；每次策略变化都能留下原因、验证结果和下一步判断，方便写探索报告。
- 实际效果：已完成文档结构补齐；本次只改文档，不改变 Harness 行为或准确率。
- 验证命令：未运行模型评测；本次验证以文档内容检查为主。
- 指标：准确率、prompt/条、completion/条、耗时均待后续实现和 API 配置后记录。
- 影响文件：`AGENT.md`、`迭代决策记录.md`。
- 下一步：按 `BASELINE_IMPLEMENTATION_PLAN.md` 的主线增强计划实现 `solution.py` 中的增强版检索分类 Harness，并在实现后补充本文件的验证结果。

### 2026-05-07 - 百炼低并发单轮验证

- 背景/触发：用户要求使用阿里百炼 API 重新跑一轮测试，并减少并发、等待时间和重试次数；同时检查当前 API 调用的网络情况。
- 决策/改动：将 `llm_client.py` 的百炼兼容接口配置改为可由环境变量覆盖；默认 endpoint 使用北京 `https://dashscope.aliyuncs.com/compatible-mode/v1`，默认模型为 `qwen3-8b`；新增 `LLM_RETRIES`、`LLM_RETRY_BASE_SLEEP`、`LLM_TIMEOUT`、`LLM_MAX_TOKENS` 配置；按百炼当前 OpenAI 兼容接口要求把 Qwen3 非思考模式参数改为 `extra_body={"enable_thinking": False}`。
- 原因：避免把 API Key 写入文件；本轮需要比默认 `workers=20`、`retries=2`、线性 1 秒退避更保守；百炼接口当前不接受旧的 `chat_template_kwargs.enable_thinking` 写法。
- 预期效果：降低限流和长等待风险，真实验证当前网络与模型调用是否稳定。
- 实际效果：三地端点均可达，未鉴权探测按预期返回 401；北京端点最快。北京真实模型调用成功，单次轻量调用总耗时约 0.999s。DEV 单轮测试完成，无 API 错误、无 prompt truncated warning；仅出现 `transformers` 的 PyTorch 缺失提示，不影响 tokenizer 计数。
- 验证命令：`DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=32 python run.py --runs 1 --workers 4`
- 指标：准确率 80.5%；prompt/条 451 token；completion/条 3.9 token；总耗时 58.0s；单轮各轮结果 80.5%。
- 影响文件：`llm_client.py`、`迭代决策记录.md`。
- 下一步：若继续调分，优先分析错误分布或尝试更稳的候选召回；若继续压测，可在当前网络状态下逐步试 `workers=6` 或 `workers=8`，但仍建议保留低重试配置。

### 2026-05-07 - 后续推进顺序调整评估

- 背景/触发：用户询问 `BASELINE_IMPLEMENTATION_PLAN.md` 第 0 节的实施顺序是否仍应在主线增强基线后机械推进。
- 决策/改动：将第 0 节从“严格按原顺序推进”改为“诊断驱动推进”：主线增强基线后先做错误类型诊断，再按瓶颈选择候选召回小步调参、confusion-aware / 候选内判别 prompt、或 token/prototype 方向。
- 原因：当前本地 DEV 纯检索诊断显示候选集 gold 命中率 94.2%，top20 召回 96.5%，fallback top1 仅 53.2%；prompt 平均 451 token、p95 543、最大约 728。结合百炼单轮准确率 80.5%，说明当前损失不只来自候选召回，且 token 预算并不紧张。
- 预期效果：避免直接投入完整 prompt compiler、label prototype 或多调用策略，把后续工作集中在更可能有收益的错误样本分析、候选内判别和混淆标签处理。
- 实际效果：已更新实施计划；尚未修改 `solution.py` 或重新跑模型评测。
- 验证命令：本次只运行本地只读诊断脚本，未调用 API；脚本实例化 `MyHarness`，对 DEV 集调用内部 `_route_task`、`_retrieve`、`_select_candidate_labels`、`_select_examples` 和 `_build_messages` 统计候选召回与 prompt token。
- 指标：DEV 539 条；候选集 gold 命中率 94.2%；top1/top3/top5/top8/top12/top16/top20 召回分别为 53.2%/75.9%/84.6%/89.6%/92.4%/95.0%/96.5%；平均候选数 13.6；平均示例数 10；prompt token 平均 451、p95 543、最大 728。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：若继续调分，优先保留一次原始预测与候选列表用于错误分型；先尝试低成本 top-K / few-shot / 示例顺序实验，再决定是否实现 confusion-aware。

### 2026-05-07 - 调用链路错误分型诊断

- 背景/触发：用户要求按错误分型诊断计划执行；在继续改 `solution.py` 前，需要确认当前 80.5% 的损失来源，避免准确率下降且无法回退。
- 决策/改动：未修改 `solution.py`；运行带追踪的低并发 API 诊断脚本，按样本记录候选命中、raw response、解析结果、fallback、prompt token、completion token、API 异常和错误类型。随后将调用链路诊断结论补入实施计划。
- 原因：无 API 诊断只能看到候选召回和预算，无法区分模型误判、解析失败、调用失败和 fallback 路径；需要真实模型回复做错误分型。
- 预期效果：明确下一步是调候选、调 prompt、修 parser、处理 API，还是推进 confusion-aware。
- 实际效果：诊断完成；没有 API 调用失败、没有 parser missed gold、没有 invalid output fallback 错误、没有 token over limit。错误主要分为候选命中但模型误判，以及候选 miss。
- 验证命令：`DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=32 python -c <diagnostic_script>`
- 指标：DEV 539 条；准确率 80.5%，正确 434，错误 105；`candidate_hit_llm_wrong=74`，`candidate_miss=31`，`api_error=0`，`invalid_output_fallback_wrong=0`，`correct_invalid_fallback=0`，`parser_missed_gold=0`；prompt token 平均 451、p95 543、最大 728；completion token 平均 3.9、p95 7、最大 9；单请求 latency 平均 0.43s、p95 0.84s、最大 3.56s；总耗时 58.1s。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：不优先处理 API、parser 或 token prototype；先做低成本 top-K / candidate 数量实验，同时更重点探索候选内判别 prompt 或轻量 contrast/confusion-aware。

### 2026-05-07 - 候选扩张与规则调参探索

- 背景/触发：用户要求继续做候选召回与 prompt 参数小步探索，并在测试时跑 4 轮以降低随机性；同时要求每次改动前确保 GitHub 远端已有可回退基线。
- 决策/改动：先确认远端 `master` 已同步到 `ad0d6ad`；随后只做内存子类/临时脚本实验，不修改 `solution.py`。三类小步实验均未优于当前基线，因此结论为“不落代码变更，保留主线增强基线”。
- 原因：调用链路错误分型显示候选命中但模型误判是主损失，但全局候选扩展、泛化 prompt 规则和检索 override 都可能增加误判；必须先证明收益再改提交文件。
- 预期效果：找到一个低风险的小步参数或 prompt 改动；如果没有收益，则明确终止候选扩张与规则调参方向，避免降低准确率。
- 实际效果：`top16 + 12 examples` 单轮准确率 79.6%；prompt-only 判别规则单轮准确率 78.7%；高置信检索 override 阈值网格在一轮基线 API 诊断中没有任何组合超过 80.5% 基线。最终未修改 `solution.py`。
- 验证命令：`DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=32 python run.py --runs 4 --workers 4`
- 指标：当前基线 4 轮平均准确率 80.4%，各轮 80.3%/80.3%/80.5%/80.3%；prompt/条 451 token；completion/条 3.9 token；总耗时 226.5s；无 API 错误；无 prompt truncated warning。只读参数扫描中，`top16 + 12 examples` 的候选命中率为 95.5%、gold 示例覆盖为 92.4%、prompt 平均 525 token，但实际 API 单轮降分。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：候选扩张与规则调参探索完成并停止；后续如继续调分，应进入更局部的 contrast/confusion-aware 小实验，每个实验先单轮验证，只有超过基线后再跑 4 轮。

### 2026-05-07 - 局部对比探索目标与单轮评测策略

- 背景/触发：用户要求以后每次评测只跑 1 轮，并根据候选扩张与规则调参结果更新下一轮探索目标和任务。
- 决策/改动：更新 `BASELINE_IMPLEMENTATION_PLAN.md`，将后续默认评测改为 `--runs 1 --workers 4`；下一轮目标收敛为“局部 contrast / 轻量 confusion-aware”，不再泛化为全局 prompt 规则或全局候选扩展。
- 原因：候选扩张与规则调参显示全局扩候选、泛化判别规则和高置信检索 override 都会降分或无收益；4 轮基线只用于稳定性确认，日常探索跑 4 轮成本高且不能改变明显负向实验的判断。
- 预期效果：降低后续探索成本，避免重复已验证无效的方向，把探索集中到候选内误判的局部对比。
- 实际效果：已更新计划文档；未修改 `solution.py`，未运行新评测。
- 验证命令：未运行；本次为文档策略调整。
- 指标：沿用候选扩张与规则调参结论作为依据：基线 4 轮平均 80.4%；`top16 + 12 examples` 单轮 79.6%；prompt-only 判别规则单轮 78.7%；高置信检索 override 无阈值超过 80.5% 基线。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：局部对比探索每个实验只先跑 1 轮；只有单轮超过当前基线区间约 80.3%-80.5%，才考虑保留代码并追加 4 轮确认。

### 2026-05-08 - 局部对比与轻量混淆探索

- 背景/触发：用户要求执行局部 contrast / 轻量 confusion-aware 探索，只针对候选内误判，不做全局 prompt 规则。
- 决策/改动：先确认远端 `master` 已同步到 `5a54189`；随后只用内存子类运行三个临时实验，不修改 `solution.py`。三个实验都低于基线，因此本轮结论为“不落代码变更，保留当前稳定基线”。
- 原因：候选扩张与规则调参已经证明全局扩候选、泛化判别规则和检索 override 无收益；本轮只能验证更局部的 contrast 是否能减少候选内误判。
- 预期效果：如果局部 contrast 能提升候选内判别，单轮准确率应超过当前基线区间约 80.3%-80.5%，且无 API 错误和 prompt 截断。
- 实际效果：三种局部实验均降分。局部 label hints 将候选近邻训练样本压缩到候选区下方，单轮 78.5%；共享 label token contrast 只对 label 名称相近候选加入局部对比，单轮 79.2%；示例顺序调整不增加 token，仅将最相关示例前置，单轮 78.5%。均不应进入 `solution.py`。
- 验证命令：三次均为 `DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=32 python -c <variant_script>`，`workers=4`。
- 指标：`label_hints` 准确率 78.5%，正确 423，prompt/条约 570，completion/条 3.9，总耗时 58.0s，API 错误 0；`shared_token_contrast` 准确率 79.2%，正确 427，prompt/条约 531，completion/条 3.9，总耗时 57.9s，API 错误 0；`examples_descending` 准确率 78.5%，正确 423，prompt/条 451，completion/条 3.9，总耗时 58.0s，API 错误 0。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：局部对比与轻量混淆探索完成并停止；除非出现新的非 DEV 定制机制，否则不继续通过 prompt 加局部信息或调整示例顺序刷 DEV，优先保留当前稳定基线并转向报告整理、提交准备或正式评测风险检查。

### 2026-05-08 - 提交风险整理与迭代记录修缮

- 背景/触发：用户要求根据截至局部对比探索的实验结果更新后续任务，并把迭代记录修缮为可复盘的探索说明：写清楚每次实验跑的 Harness、分析结果、探索主线之间为什么切换，以及为什么某些改动不落代码、不继续探索。
- 决策/改动：更新 `迭代决策记录.md`，新增核心原则、探索主线总览、主线推进逻辑；同步更新 `BASELINE_IMPLEMENTATION_PLAN.md`，把后续工作从默认 `label prototype / concept memory` 改为提交前风险检查、报告整理和提交准备。未修改 `solution.py`。
- 原因：当前 prompt 平均 451、p95 543、最大约 728，距离 2048 限制很远；候选扩张与规则调参、局部对比与轻量混淆多个变体都证明“更多候选、更多规则、更多局部提示、重排示例”没有带来收益，继续沿 DEV 细刷容易过拟合。label prototype 的价值主要是缓解 token 压力或压缩大量 label memory，目前没有触发条件。
- 预期效果：后续读文档的人可以直接看出：稳定基线是什么、每个负向实验为什么被否决、探索目标为什么从调分转为提交风险控制，以及哪些方向只作为条件预案保留。
- 实际效果：文档层面完成修缮；Harness 行为和准确率不变。
- 验证命令：未运行模型评测；本次为文档修订。修订后检查 `git diff --check` 和 API key 泄露扫描。
- 指标：沿用已有实验作为依据：基线单轮 80.5%；基线四轮平均 80.4%；候选扩张与规则调参变体为 79.6%/78.7%/无 override 阈值超过基线；局部对比与轻量混淆变体为 78.5%/79.2%/78.5%；全程无 API 错误、无 prompt truncated warning。
- 影响文件：`BASELINE_IMPLEMENTATION_PLAN.md`、`迭代决策记录.md`。
- 下一步：执行提交前风险检查：确认 `solution.py` 不含 API key、本地文件依赖或 DEV 领域硬规则；确认 prompt 预算和 allowlist fallback 仍有效；整理探索报告时保留负向实验结论，避免把未验证收益的复杂方案写成已采用方案。

### 2026-05-08 - Listwise 候选重排探索

- 背景/触发：用户指出最终竞赛分数由正确率和探索迭代文档共同组成，希望增加有边界的方案探索，并优先探索 Listwise Reranking Harness。
- 决策/改动：先确认远端 `master` 已同步到 `09a40e8`；随后创建临时 `/tmp/listwise_eval.py` 评测脚本，用内存子类继承当前 `MyHarness`，只覆盖 listwise prompt、候选数量和示例数量，不修改 `solution.py`。共跑 3 个单轮变体：同候选 listwise、更多候选少示例 listwise、候选-only listwise。
- 原因：Listwise 的理论优势是把“从全部 label 中分类”改成“对检索出的候选 label 做排序并取 rank-1”，可能降低候选命中后的 LLM 误判；同时它比 pairwise tournament 便宜，只需一次 LLM 调用。
- 预期效果：如果 Qwen3-8B 能稳定处理候选列表排序，同候选 listwise 应至少接近当前 80.3%-80.5% 基线；更多候选少示例版本应验证 listwise 是否能承接更大的候选集；候选-only 版本用于判断 label 名称本身是否足以支持 reranking。
- 实际效果：三个变体均明显低于当前基线。`same_candidates` 为 77.7%，`more_candidates_less_examples` 为 77.4%，`candidates_only` 为 68.6%。全程无 API 错误、无 prompt 截断，说明失败不是调用或预算问题。
- 验证命令：`DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=64 python /tmp/listwise_eval.py --workers 4 --variants same_candidates more_candidates_less_examples candidates_only`
- 指标：`same_candidates` 正确 419/539，准确率 77.737%，prompt/条 514，completion/条 3.993，耗时 58.9s，截断 0，API/脚本错误 0，非法输出 fallback 0；`more_candidates_less_examples` 正确 417/539，准确率 77.365%，prompt/条 429，completion/条 4.056，耗时 56.3s，截断 0，API/脚本错误 0，非法输出 fallback 2；`candidates_only` 正确 370/539，准确率 68.646%，prompt/条 273，completion/条 5.685，耗时 56.5s，截断 0，API/脚本错误 0，非法输出 fallback 1。
- 分析：同候选 listwise 在候选集和 evidence 基本一致的情况下仍从基线降到 77.7%，说明主要损失来自 listwise/rerank 话术本身，而不是候选召回。更多候选少示例没有改善，延续了“候选变多会增加判断噪声”的结论。候选-only 大幅降到 68.6%，说明当前 DEV label 名称不足以表达完整类别语义，few-shot evidence 仍是必要信息。
- 决策：Listwise Reranking Harness 不落入 `solution.py`，不追加 4 轮确认；它作为已探索但不采用方向写入报告。后续如果继续探索 reranking，应改成更低风险的局部二选一或只对低置信样本条件触发，而不是默认把所有样本改成 listwise ranking 任务。
- 影响文件：`迭代决策记录.md`。
- 下一步：保持主线增强基线不变；继续探索时优先选择机制足够不同、能解释泛化收益且单轮成本可控的方向，并沿用“单轮明显超过基线才考虑落代码”的规则。

### 2026-05-08 - Label Prototype / Concept Memory 探索

- 背景/触发：用户要求继续探索 Label Prototype / Concept Memory；同时竞赛评分包含探索迭代文档，因此需要记录正负结果、分析和是否落代码的决策依据。
- 决策/改动：先确认远端 `master` 已同步到 `df861c4`；随后创建临时 `/tmp/prototype_eval.py`，用内存子类继承当前 `MyHarness`，只覆盖 prototype 生成、候选数量、示例数量和 prompt 组织。探索 3 个单轮变体后，将唯一稳定超过基线的“更多候选 + label memory cards + 4 examples”落入 `solution.py`。
- 原因：prototype 的理论价值不是让 LLM 编 label 描述，而是从训练样本中压缩出可复用的 label memory。这样可能用更少 examples 支撑更多候选 label，减少候选 miss，同时保留原始训练样本证据，避免纯 label 名称判断。
- 预期效果：如果 prototype 有效，替代 examples 或补充 cues 至少应接近 80.3%-80.5% 基线；如果它真正缓解候选召回与候选内判别的冲突，则“更多候选 + 少量 examples + memory cards”应超过基线且不触发 token 截断。
- 实际效果：三个变体分化明显。`cards_replace_examples` 单轮 78.108%，说明 prototype 不能替代原始 examples；`inline_cues_plus_examples` 单轮 78.850%，说明轻量 cues 加法仍会增加噪声；`cards_more_candidates` 单轮 80.891%，随后 4 轮确认全部为 80.891%，无 API 错误、无 prompt 截断、无非法输出 fallback。落入 `solution.py` 后，官方 `run.py --runs 1 --workers 4` 验证为 80.9%。
- 验证命令：探索命令为 `DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=64 python /tmp/prototype_eval.py --workers 4 --variants cards_replace_examples inline_cues_plus_examples cards_more_candidates`；确认命令为同配置下 `python /tmp/prototype_eval.py --workers 4 --variants cards_more_candidates --runs 4`；落代码验证命令为同配置下 `python run.py --runs 1 --workers 4`。
- 指标：`cards_replace_examples` 正确 421/539，准确率 78.108%，prompt/条 807，completion/条 3.859，耗时 59.3s；`inline_cues_plus_examples` 正确 425/539，准确率 78.850%，prompt/条 620，completion/条 3.850，耗时 55.3s，非法输出 fallback 1；`cards_more_candidates` 探索单轮正确 436/539，准确率 80.891%，prompt/条 1074，completion/条 3.911，耗时 61.8s。4 轮确认均为 436/539、80.891%，prompt/条 1074，无截断、无错误、无非法输出 fallback。落代码后官方单轮为 80.9%，prompt/条 1074，completion/条 3.9，总耗时 62.5s。
- 分析：prototype 单独替代 examples 会压缩掉细节，效果低于基线；只在候选行加入 cues 也会产生额外噪声。正向收益来自组合：把候选从 12 扩到 16，低置信时仍可到 20；examples 从 10 降到 4；每个候选用代码生成的 cues 和代表样本片段补充 label 概念。它不是 DEV 领域硬规则，不调用 LLM 生成描述，也不依赖本地文件；风险是 prompt 成本从 451/条升到 1074/条，私有集若 label 更多或文本更长，需要依赖现有预算降级兜底。
- 决策：将 `cards_more_candidates` 方案落入 `solution.py`。不采用 `cards_replace_examples` 和 `inline_cues_plus_examples`，也不把 prototype 扩展成 LLM 生成描述。当前主线变为“检索候选 + label memory cards + 少量 evidence examples + 单次 LLM 分类”。
- 影响文件：`solution.py`、`迭代决策记录.md`、`BASELINE_IMPLEMENTATION_PLAN.md`、`AGENT.md`。
- 下一步：后续提交前重点检查 prototype prompt 在私有集长文本、更多 label 或选择题任务上的预算降级是否稳；若继续探索，应优先做条件触发或 token 压缩，而不是继续堆更多 label card 内容。

### 2026-05-08 - 共享数据侧增强审核

- 背景/触发：用户拿到参赛者共享数据 `data/data.zip`，要求审核数据是否有用、是否会折损泛化性，并对数据侧增强做探索。
- 决策/改动：先确认当前 HEAD `d80579d` 已推送到远端 `master`；随后只读检查 zip 清单、字段、重复、跨 split 重叠和标签空间。将新增 MCQ/OOD 数据作为独立验证集运行当前 `solution.py`，再测试两种数据增强：直接把 `train_mcq/train_ood` 合并到 DEV 训练流、只把与 DEV 同名 label 的外部样本合并到 DEV 训练流。未修改 `solution.py`。
- 原因：共享数据的主要风险不是格式错误，而是 label 空间与当前 DEV 不一致；如果作为训练增强直接进入 `update()` 记忆，会扩大 allowlist、污染候选集，并让 LLM 在无关 label 中选择。`solution.py` 也不能读写磁盘文件，正式提交不应依赖 `data.zip` 或硬编码共享样本。
- 预期效果：如果共享数据有泛化验证价值，当前 Harness 应能在 MCQ/OOD 独立任务上给出可解释表现；如果数据增强安全，合并共享 train 后 DEV 至少不应低于当前 80.9% 基线，候选 gold 命中也不应下降。
- 实际效果：数据格式干净：zip 中 `train_dev/test_dev` 与本地 DEV 完全一致；新增 `train_mcq/test_mcq` 和 `train_ood/test_ood` 均为 `text/label` JSONL，无重复文本、无 train/test 文本重叠、与 DEV 文本无重叠。当前 Harness 在 MCQ 独立任务单轮 73.2%，在 OOD 独立任务单轮 78.0%，说明它们适合作为外部分布验证集。直接合并共享 train 后 DEV 降到 80.1%，离线诊断显示纯外部 label 出现在 468/539 个 DEV 样本候选中，平均每条 2.91 个，gold 候选命中从 515/539、95.55% 降到 508/539、94.25%。只合并同名 `exchange_rate` 的 3 条外部样本后 DEV 单轮到 81.1%，但 gold 候选命中不变，收益只有约 1 个样本且证据薄。
- 验证命令：审核命令为 `python -c <zip_audit_script>`；独立验证为 `DASHSCOPE_API_KEY=<redacted> ... python run.py --runs 1 --workers 4 --train /tmp/harness_shared_data/train_mcq.jsonl --dev /tmp/harness_shared_data/test_mcq.jsonl` 和同配置 OOD 评测；增强验证为同配置下分别使用 `/tmp/harness_shared_data/train_dev_plus_shared_train.jsonl` 与 `/tmp/harness_shared_data/train_dev_plus_same_label_shared_train.jsonl` 评测 DEV。
- 指标：MCQ：train 96、dev 384、4 个 label，准确率 73.2%，prompt/条 628，completion/条 1.0，总耗时 42.0s。OOD：train 186、dev 600、52 个 label，准确率 78.0%，prompt/条 1063，completion/条 2.5，总耗时 70.7s。直接合并增强：train 513、label 132，DEV 准确率 80.1%，prompt/条 1112，completion/条 3.9，总耗时 67.4s。仅同名 label 增强：train 234，只新增 3 条 `exchange_rate`，DEV 准确率 81.1%，prompt/条 1074，completion/条 3.9，总耗时 72.3s。各次运行均无 API 错误、无 prompt truncated warning。
- 分析：MCQ/OOD 对探索报告有价值，因为它们检验了选择题路由、长文本 prompt、非银行多域 label 和 prototype 的预算风险；但它们不适合作为当前 DEV 的直接训练增强。直接合并导致 label allowlist 从 77 扩到 132，外部 label 会进入候选和 label memory cards，既降低候选 gold 命中，也提高 prompt 噪声。同名 label 增强的 81.1% 不能作为落代码依据：样本只有 3 条，全部是 `exchange_rate`，可能只是当前公开 DEV 对该概念的偶然贴合；而且正式提交不能读取 `data.zip`，硬编码共享样本也会削弱 Harness 自适应叙事并增加复核风险。
- 决策：共享数据不进入 `solution.py`，不作为默认训练增强。保留它的用途为：1. 外部分布验证集；2. 探索报告中的数据侧增强负例和保守增强观察；3. 后续检验路由或 token 降级是否会伤害 MCQ/OOD。若继续做数据侧方向，只做训练流内自适应策略，例如自动识别选择题、按 label_count 调整 candidate/example/card 预算，不能读取外部文件、不能硬编码共享数据、不能把共享 test label 用作训练。
- 影响文件：`迭代决策记录.md`。
- 下一步：如继续探索，应基于 MCQ/OOD 做“验证当前机制的泛化风险”，而不是把共享数据并入提交 Harness。可以优先分析 MCQ 73.2% 的错误类型，判断选择题路由是否需要独立 prompt；但任何改动仍要同时回测 DEV，避免为 MCQ/OOD 牺牲当前主线。

### 2026-05-08 - 隔离式 MCQ 路由探索

- 背景/触发：共享数据审核显示当前最好基线在 MCQ 独立任务上只有 73.2%，低于 DEV 80.9% 和 OOD 78.0%；用户要求做隔离式 MCQ 路由实验，并做好失效后回退机制。
- 决策/改动：先确认远端 `master` 已同步到 `06517f4`；随后用内存子类测试 3 个只影响 MCQ 的路由变体。选择唯一正向的“无示例直接选择题 prompt”落入 `solution.py`：当训练 label 是 A-H 单字母选项且文本含选项结构时，先调用专用 MCQ prompt；若不满足触发条件、prompt 超预算、API 调用异常或输出无法解析为 allowed option，则返回 `None` 并继续原通用 `MyHarness` 路径。
- 原因：MCQ 的 label `A/B/C/D` 没有类别语义，训练 examples 跨主题且易干扰；更合适的 Harness 行为是把它识别成选择题阅读任务，让 LLM 直接读题干和选项。为了避免牺牲 DEV，该分支必须只由 label set 和文本结构共同触发，并保留原通用路径作为回退。
- 预期效果：MCQ 单轮应明显超过共享数据审核时的 73.2%，prompt 成本应降低；DEV 因 label set 不符合 A-H 选择题，不应触发该分支，准确率应保持 80.9% 附近。
- 实际效果：临时实验中，`direct_no_examples` 为 74.740%，正确 287/384，prompt/条 192，无回退、无非法输出；`parse_options` 为 71.354%，正确 274/384，1 次非法输出回退；`one_per_option_example` 为 70.573%，正确 271/384。落入 `solution.py` 后用官方 `run.py` 验证，MCQ 单轮为 75.5%，prompt/条 189；DEV 单轮保持 80.9%，prompt/条 1074。无 API 错误、无 prompt truncated warning。
- 验证命令：探索命令为 `DASHSCOPE_API_KEY=<redacted> DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 DASHSCOPE_MODEL=qwen3-8b LLM_RETRIES=0 LLM_RETRY_BASE_SLEEP=0.2 LLM_TIMEOUT=20 LLM_MAX_TOKENS=64 python -c <isolated_mcq_variant_script>`；落代码后验证命令为同配置下 `python run.py --runs 1 --workers 4 --train /tmp/harness_shared_data/train_mcq.jsonl --dev /tmp/harness_shared_data/test_mcq.jsonl` 和 `python run.py --runs 1 --workers 4 --train data/train_dev.jsonl --dev data/test_dev.jsonl`。
- 指标：临时 `direct_no_examples`：74.740%，287/384，prompt/条 192，completion/条 1.206，耗时 42.3s，回退 0，截断 0。临时 `parse_options`：71.354%，274/384，prompt/条 213，completion/条 1.185，耗时 38.8s，非法输出回退 1。临时 `one_per_option_example`：70.573%，271/384，prompt/条 347，completion/条 1.010，耗时 38.8s，回退 0。落代码 MCQ：75.5%，prompt/条 189，completion/条 1.5，总耗时 42.0s。落代码 DEV：80.9%，prompt/条 1074，completion/条 3.9，总耗时 65.8s。
- 分析：无示例 MCQ prompt 的收益来自减少干扰，而不是增加知识。`parse_options` 的显式“先解析再比较”话术让小模型更不稳；每选项一条训练例会把无关题目带入上下文，降分更明显。这与之前候选扩张、局部 hint 和 listwise 的负向结论一致：对 Qwen3-8B，额外结构化信息不一定帮助，低噪声任务表述更重要。
- 决策：将隔离式无示例 MCQ 路由落入 `solution.py`。不采用 `parse_options` 和 `one_per_option_example`。后续不继续堆 MCQ examples 或复杂解析规则；若再优化 MCQ，必须保持条件触发和回退，并同时回测 DEV。
- 影响文件：`solution.py`、`迭代决策记录.md`、`BASELINE_IMPLEMENTATION_PLAN.md`、`AGENT.md`。
- 下一步：保留当前主线：普通/多 label 任务使用 label memory cards + 少量 evidence examples，选择题任务使用隔离式无示例 prompt。提交前重点检查不读外部文件、不硬编码共享数据、fallback 仍只返回 allowed label。
